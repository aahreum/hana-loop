# 작업 로그 — 2025-05-07 세션 01

> 세션 목표: Supabase 스키마 설계 + 기반 타입/API 구조 세팅

---

## 작업 요약

| #   | 작업                                                                  | 소요 시간 |
| --- | --------------------------------------------------------------------- | --------- |
| 1   | 환경 변수 정리 (브라우저 Supabase 클라이언트 제거)                    | ~2분      |
| 2   | Supabase 스키마 설계 및 마이그레이션 파일 작성                        | ~10분     |
| 3   | 시드 데이터 작성 (CT-045 Excel 원본 + 배출계수)                       | ~5분      |
| 4   | 타입 시스템 업데이트 (activity, factor, emission, database)           | ~5분      |
| 5   | Post 타입 및 마이그레이션 추가                                        | ~3분      |
| 6   | fake backend 구조 설계 및 `api.ts` / `_store.ts` 작성                 | ~5분      |
| 7   | docs 업데이트 (02, 06, 08)                                            | ~5분      |
| 8   | Gemini 코드리뷰 1차 반영 (api.ts, globals.css, CLAUDE.md, \_store.ts) | ~5분      |
| 9   | API Routes 전체 구현 (7개 엔드포인트)                                 | ~10분     |
| 10  | database.ts Relationships 필드 추가 + CI 빌드 env 수정                | ~3분      |
| 11  | Gemini 코드리뷰 2차 반영 (RPC 트랜잭션, scope 정리, CASCADE 삭제)     | ~5분      |
| 12  | docs/wiki 동기화 (02-data-model, 08-backend-setup, 위키 API-설계)     | ~3분      |

---

## 사용자 요청 흐름

1. **"supabase 스키마 생성하고 기반 세팅부터 하려고 해"**
   → 환경 변수부터 정리. `NEXT_PUBLIC_SUPABASE_ANON_KEY` 제거, `SUPABASE_URL`로 변경.

2. **CT-045 Excel 데이터 제공 + "스키마 어떻게 생성하면 좋을까"**
   → 4개 테이블 설계 (companies, emission_factors, activities, emission_results).
   → Excel 원본 날짜(YYYY-MM-DD)를 `date` 컬럼으로 저장, `year_month`는 generated column으로 파생.
   → `factor_category`를 activities에 추가해 배출계수 매핑 명시화.

3. **외부 spec(Company/GhgEmission/Post/fake backend) 제시 + "이것도 있어야 해"**
   → `Post` 타입 및 마이그레이션 추가.
   → `GhgEmission`은 DB 저장 타입이 아닌 API 응답 변환 타입으로 정의.
   → fake backend = Next.js API Route 안에 jitter/maybeFail 적용 (별도 서버 불필요).

4. **"Supabase 연결하되 시뮬레이션 API도 필요해. 어떤 키 써야 해?"**
   → 인메모리 스토어 제거, `_store.ts`를 시뮬레이션 유틸만 담는 파일로 정리.
   → Supabase 신규 키 체계 설명: Secret key (`sb_secret_...`) = 구 service_role key → 사용.

5. **"Gemini 코드리뷰 반영해줘"**
   → api.ts 헤더 병합 순서 수정 + 204 No Content 대응.
   → globals.css `--font-pretendard` CSS 변수 추가.
   → CLAUDE.md jitter 예시 코드 수정 (숫자 반환 → Promise 반환).
   → \_store.ts에 `toCC` / `toCCArray` snake_case→camelCase 변환 유틸 추가.

6. **"sql 실행했어. 다음 작업 뭐해야 하는지 파악해줘"**
   → Supabase DB 구축 확인 후 API Routes 전체 구현 착수.

7. **"제미나이 코드리뷰 반영해주고 PR에 코멘트 남겨줘" (PR #6)**
   → RPC 트랜잭션, scope 정리, CASCADE 삭제 최적화 반영.

8. **"지금 변경한것도 PR에 커맨드 남겨주고 log 업데이트 해줘"**
   → docs/02-data-model, docs/08-backend-setup, 위키 API-설계 동기화 후 PR 코멘트 작성.

---

## 주요 결정 및 트레이드오프

### `activities.date` vs `yearMonth`

| 방향                                   | 장점                                                           | 단점                                      |
| -------------------------------------- | -------------------------------------------------------------- | ----------------------------------------- |
| `yearMonth` (기존)                     | 단순, 조회 빠름                                                | Excel 원본 날짜 손실, 추후 일별 분석 불가 |
| **`date` + generated `year_month`** ✅ | Excel 원본 보존, 일별 분석 가능, Excel 직접 임포트 보너스 대응 | 컬럼 추가, 마이그레이션 복잡도 소폭 증가  |

→ **`date` 선택.** 과제 보너스 조건(Excel 직접 임포트 인터페이스) 대응을 위해.

### `factor_category` 위치

| 방향                                     | 장점                             | 단점                                       |
| ---------------------------------------- | -------------------------------- | ------------------------------------------ |
| API에서 type+description으로 자동 매핑   | activities 단순                  | 매핑 규칙 변경 시 과거 결과 달라질 수 있음 |
| **`activities.factor_category` 명시** ✅ | 어떤 계수를 썼는지 불변으로 기록 | 활동 생성 시 factor 선택 UI 필요           |

→ **명시 선택.** activities는 불변(immutable) 원칙. 어떤 배출계수로 계산했는지 추적 가능해야 함.

### 가짜 백엔드 위치

| 방향                                        | 장점                                   | 단점                            |
| ------------------------------------------- | -------------------------------------- | ------------------------------- |
| 별도 `fakeApi.ts` 인메모리 서버             | spec 예시와 동일                       | Supabase와 이중 구현, 코드 중복 |
| **Next.js API Route + jitter/maybeFail** ✅ | Supabase 실제 데이터 사용, 코드 단일화 | spec 예시와 구조 다름           |

→ **API Route 선택.** 실제 Supabase에 연결하되 네트워크 시뮬레이션만 API Route 레이어에서 적용. 과제 의도(loading/error/rollback 테스트)를 만족하면서 실제 DB도 사용.

---

## 생성/수정 파일 목록

```
새로 생성:
  supabase/migrations/20250507000000_initial_schema.sql
  supabase/migrations/20250507000001_add_posts.sql
  supabase/seed.sql
  src/app/api/_store.ts
  src/app/api/companies/route.ts
  src/app/api/activities/route.ts
  src/app/api/activities/[id]/route.ts
  src/app/api/factors/route.ts
  src/app/api/emission-results/route.ts
  src/app/api/posts/route.ts
  src/app/api/posts/[id]/route.ts
  src/shared/lib/api.ts
  src/shared/types/post.ts
  docs/00-assignment.md
  log/2025-05-07-session-01.md

수정:
  .env.local.example                  (키 체계 문서화)
  .github/workflows/build.yml         (빌드용 플레이스홀더 env 추가)
  .github/workflows/ci.yml            (빌드용 플레이스홀더 env 추가)
  src/app/globals.css                 (--font-pretendard CSS 변수 추가)
  src/shared/lib/supabase.server.ts   (NEXT_PUBLIC_SUPABASE_URL → SUPABASE_URL)
  src/shared/types/activity.ts        (yearMonth → date, factorCategory 추가)
  src/shared/types/factor.ts          (name, activityType 추가)
  src/shared/types/emission.ts        (GhgEmission 집계 타입 추가)
  src/shared/types/database.ts        (실제 스키마 반영, posts 추가, Relationships 필드 추가)
  data/seed.ts                        (CT-045 원본 데이터로 교체, posts 추가)
  CLAUDE.md                           (과제 스펙 링크 추가, jitter 예시 수정)
  docs/02-data-model.md
  docs/06-api-client.md
  docs/08-backend-setup.md

삭제:
  src/shared/lib/supabase.ts          (브라우저 클라이언트 — 서버 전용 아키텍처로 불필요)
```

---

## 오류 수정 이력

### SQL 오류

| 오류                                                 | 원인                                                       | 수정                                                                  |
| ---------------------------------------------------- | ---------------------------------------------------------- | --------------------------------------------------------------------- |
| `generation expression is not immutable`             | `to_char`, `LEFT(date::text)` 모두 STABLE — DateStyle 의존 | Generated column → BEFORE INSERT trigger (`fn_set_year_month`)로 교체 |
| `invalid input syntax for type uuid: "p0000000-..."` | `p`는 16진수 아님 (UUID는 0-9, a-f만 허용)                 | `p0000000` → `b0000000` (seed.sql, data/seed.ts 동시)                 |

### TypeScript / CI 오류

| 오류                                         | 원인                                                                                         | 수정                                                            |
| -------------------------------------------- | -------------------------------------------------------------------------------------------- | --------------------------------------------------------------- |
| insert 파라미터 타입이 `never[]`로 추론      | `database.ts` 각 테이블에 `Relationships: []` 누락 — supabase-js v2 `GenericTable` 필수 필드 | 전 테이블에 `Relationships: []` 추가                            |
| CI 빌드 실패 `supabaseUrl is required`       | `supabaseAdmin`이 모듈 로드 시 초기화되는데 CI에 env 없음                                    | `.github/workflows/build.yml`, `ci.yml`에 플레이스홀더 env 추가 |
| PR 충돌 — `chore/#1` 머지 후 `chore/#3` 충돌 | 동일 베이스에서 분기한 두 브랜치가 같은 파일 수정                                            | `git merge origin/main -X ours`로 우리 버전 우선 해소           |

### Gemini 코드리뷰 1차 반영 (PR #4)

| 항목                        | 파일                    | 수정 내용                                  |
| --------------------------- | ----------------------- | ------------------------------------------ |
| camelCase/snake_case 불일치 | `src/app/api/_store.ts` | `toCC<T>` / `toCCArray<T>` 변환 유틸 추가  |
| jitter 예시 오류            | `CLAUDE.md`             | Promise 반환 형태로 수정                   |
| --font-pretendard 미정의    | `src/app/globals.css`   | `:root`에 변수 추가                        |
| 헤더 병합 + 204 대응        | `src/shared/lib/api.ts` | spread 순서 수정, `res.text()` 조건부 파싱 |

### Gemini 코드리뷰 2차 반영 (PR #6)

| 항목                                | 파일                                         | 수정 내용                                                             |
| ----------------------------------- | -------------------------------------------- | --------------------------------------------------------------------- |
| [high] 트랜잭션 미보장              | `migrations/…002.sql`, `activities/route.ts` | 3단계 개별 쿼리 → `create_activity_with_emission` RPC (단일 트랜잭션) |
| [high] emission_results 에러 미처리 | —                                            | RPC 전환으로 해결 (PostgreSQL 예외 → 자동 롤백)                       |
| [medium] scope 중복                 | `activity.ts`                                | `CreateActivitySchema`에서 scope 제거 → `ActivitySchema`에 명시 이동  |
| [medium] CASCADE 미활용             | `activities/[id]/route.ts`                   | 수동 삭제 코드 제거 (이미 `ON DELETE CASCADE` 설정됨)                 |

---

## 과제 스펙 문서화 및 프로세스 정비

| 작업                         | 내용                                                          |
| ---------------------------- | ------------------------------------------------------------- |
| `docs/00-assignment.md` 생성 | 과제 원본 스펙 전체 정리                                      |
| `CLAUDE.md` 업데이트         | 최상단에 `docs/00-assignment.md` 링크 추가                    |
| `/ship` 커맨드 업데이트      | Step 0 추가 — 과제 스펙 & CLAUDE.md 위반 사전 체크            |
| `supabase/` gitignore 미추가 | 마이그레이션 파일이 PostgreSQL 설계 능력 증거, 가점 요소 해당 |

---

## PR 이력

| PR  | 브랜치                                      | 내용                                                           |
| --- | ------------------------------------------- | -------------------------------------------------------------- |
| #2  | `chore/#1-initialize-project-foundation`    | 프로젝트 초기화 (Next.js, ESLint, Husky, CI)                   |
| #4  | `chore/#3-supabase-schema-types-api-client` | Supabase 스키마, 타입 시스템, API 클라이언트, 과제 스펙 문서화 |
| #6  | `feat/#5-api-routes`                        | API Routes 전체 구현 + Gemini 2차 리뷰 반영 + docs 동기화      |

---

## 다음 세션에서 할 일

- [x] Supabase SQL 오류 수정 완료
- [x] Supabase 프로젝트에 마이그레이션 + 시드 실행 확인
- [x] API Routes 구현 (`/api/activities`, `/api/companies`, `/api/posts`, `/api/factors`, `/api/emission-results`)
- [x] PR #6 머지
- [x] shadcn/ui 컴포넌트 설치 (Button, Input, Select, Badge, Dialog 등)
- [x] 레이아웃 구현 (사이드바 + 헤더)
- [x] 대시보드 KPI 카드 (총 배출량, 전월 대비, 최대 배출원, Scope별)
- [x] 차트 (월별 라인, Scope별 파이/도넛, Stacked Bar)
- [x] 활동 데이터 테이블
- [x] 활동 입력 폼 + 유효성 검사 + 에러 처리
- [x] HanaLoop 브랜드 컬러 적용 (#308af9 블루 primary, #4bb074 그린, 그라데이션)
- [ ] **다크모드/라이트모드 토글 버튼** — 헤더에 Sun/Moon 아이콘 토글, `<html>` class 전환 (`dark`/`light`), Zustand uiStore에 theme 상태 추가
